'use client';
import {useEffect,useState} from 'react'; import {Bell,Command,HelpCircle,Search,X} from 'lucide-react';
interface P{title?:string;subtitle?:string;onSearchChange?:(v:string)=>void;searchValue?:string}
export function Navbar({title='Overview',onSearchChange,searchValue=''}:P){const[open,setOpen]=useState(false);useEffect(()=>{const h=(e:KeyboardEvent)=>{if((e.metaKey||e.ctrlKey)&&e.key.toLowerCase()==='k'){e.preventDefault();setOpen(true)}if(e.key==='Escape')setOpen(false)};addEventListener('keydown',h);return()=>removeEventListener('keydown',h)},[]);return <><header className="topbar"><div className="breadcrumb"><span>Billing</span><i>/</i><b>{title}</b></div><div className="command-search" onClick={()=>!onSearchChange&&setOpen(true)}><Search size={16}/><input aria-label="Global search" value={searchValue} onChange={e=>onSearchChange?.(e.target.value)} placeholder={onSearchChange?'Search records...':'Search invoices, customers, payments...'}/><kbd><Command size={11}/> K</kbd></div><div className="top-actions"><span className="environment"><i/>Live</span><button aria-label="Help"><HelpCircle size={17}/></button><button className="notification" aria-label="Notifications"><Bell size={17}/><i/></button><div className="avatar">AD</div></div></header>{open&&<div className="command-palette" onMouseDown={()=>setOpen(false)}><div className="command-dialog" role="dialog" aria-modal="true" onMouseDown={e=>e.stopPropagation()}><div className="flex items-center"><input autoFocus placeholder="Search invoices, customers, payments..."/><button className="p-3 text-slate-400" onClick={()=>setOpen(false)} aria-label="Close"><X size={16}/></button></div><p>Quick actions</p>{['Create invoice','Record payment','Add customer','Export report'].map(x=><button key={x} className="command-option w-full text-left">{x}<span>↵</span></button>)}<p>Recent</p>{['Invoices','Customers','Payments','Settings'].map(x=><button key={x} className="command-option w-full text-left">{x}</button>)}</div></div>}</>}
